# luigi-web runtime dependencies. Pinned.
#
# HTMX 1.9.12 and SortableJS 1.15.2 are served locally from static/js/vendor
# so LAN-only installs do not depend on public CDNs. The official Copilot SDK
# replaces the retired GitHub Models inference endpoint.

fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
sqlalchemy==2.0.34
psycopg[binary]==3.2.1
python-multipart==0.0.9
httpx==0.27.2
github-copilot-sdk==1.0.11

# Game'N'Watch integration (reads/writes the bot's Google Sheet). Optional at
# runtime — gnw.py degrades gracefully if these are missing — but installed so
# the Games/Shows tabs work out of the box.
gspread==6.1.2
google-auth==2.34.0
