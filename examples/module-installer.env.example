# Dedicated worker configuration, never application credentials.
# Keep the host and worker DATA_DIR and repository policy identical.
LUIGI_WEB_DATA_DIR=/opt/luigi-web/data
LUIGI_WEB_MODULE_REPOSITORY_OWNERS=
LUIGI_WEB_MODULE_REPOSITORIES_FILE=